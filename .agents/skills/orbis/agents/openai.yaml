interface:
  display_name: "Orbis"
  short_description: "커밋 클로저 단위 코딩을 native Codex Worker로 조율"
  default_prompt: "$orbis를 사용해 이 코딩 변경을 범위가 정해진 Worker로 조율합니다."

policy:
  allow_implicit_invocation: false
